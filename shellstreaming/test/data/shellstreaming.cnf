[worker]
# user = unix_user

# ','-separated host names
hosts = gueze

port = 18871

# One of DEBUG, INFO, WARN, ERROR, CRITICAL, FATAL.
log_level = DEBUG

# Max log file size to make rotate files
max_log_bytes = 1000000
